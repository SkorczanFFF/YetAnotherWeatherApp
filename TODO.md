# TODO

## Cloud spawn area

- **Dynamic cloud spawn area based on screen width**  
  On 1080p the current spawn area is good; on 1440p (and larger) users see empty space without clouds. Make the cloud spawn bounds scale with viewport/screen size (e.g. use camera aspect or window dimensions so wider/taller resolutions get a proportionally larger spawn volume and no visible gaps).
